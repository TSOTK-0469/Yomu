# Yomu 本地图片阅读

Yomu 将用户授权的本地目录组织为可独立阅读和分类的画册。本词汇表用于统一产品与代码中的领域语言。

## Language

**挂载源（Mount Source）**：
用户授权给应用访问的目录树，是扫描、刷新和取消挂载的边界；一个挂载源可以发现多个画册。
_Avoid_：文件夹、画册

**无法访问的挂载源（Unavailable Mount Source）**：
授权或存储暂时不可用、但仍保留画册索引和书架关系的挂载源。
_Avoid_：已取消挂载、已删除目录

**画册（Album）**：
挂载源中直接包含图片的目录，是主页展示、阅读进度、封面和分类的独立对象。
_Avoid_：挂载文件夹、挂载源

**书架（Bookshelf）**：
用户建立的画册分类。一个画册可以同时属于多个书架，删除书架不会删除或隐藏其中的画册。
_Avoid_：标签、标签集、分类夹

**默认书架（All albums）**：
系统维护的全部未隐藏画册视图。它不能重命名、删除或手动改变成员关系。
_Avoid_：默认标签、自建书架

**隐藏画册（Hidden album）**：
仍属于有效挂载源、但被用户从所有书架视图中排除的画册。
_Avoid_：已卸载画册、已删除画册

**挂载模式（Mount mode）**：
决定挂载源发现画册范围的规则。非递归模式只考虑挂载源根目录本身，递归模式考虑根目录及其所有后代目录。
_Avoid_：扫描层级

**阅读位置（Reading position）**：
画册中最后阅读图片的身份引用。若该图片不再存在，阅读位置回退到画册首张图片。
_Avoid_：页码进度、进度指针
